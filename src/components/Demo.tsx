import React from "react";

import classNames from "classnames/bind";
import styles from "./Demo.module.scss";

const cx = classNames.bind(styles);

const Demo = () => {
  return <div className={cx("container")}>asdasdasdasd</div>;
};

export default Demo;
